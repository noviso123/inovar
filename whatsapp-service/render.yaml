services:
  - type: web
    name: whatsapp-service
    env: go
    buildCommand: go build -o main .
    startCommand: ./main
    envVars:
      - key: PORT
        value: 10000
      - key: DATABASE_URL
        sync: false
      - key: WA_API_KEY
        sync: false
